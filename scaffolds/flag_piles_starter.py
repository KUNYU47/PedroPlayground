from pedro import *


def main():
    """
    Mission: There are 5 piles of flags across the surface.
    Each pile has 3 flags in a row. Pick up every flag.
    """
    # TODO: Use a for loop to visit all 5 piles
    #       Inside: another for loop to pick up 3 flags in a row,
    #       then move past the gap to the next pile

if __name__ == '__main__':
    main()
